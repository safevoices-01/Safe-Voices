# Changesets

This repository uses [Changesets](https://github.com/changesets/changesets) for internal version tracking.

## Common commands

- Create a changeset: `pnpm changeset`
- Apply version bumps: `pnpm version-packages`
- View pending release state: `pnpm changeset status`

Packages are currently private, so publishing is intentionally not part of the default workflow.
